/*
read '*' as 'value at'
to store address of datatypes we use pointers 
declaration : datatype *p_var = &var  ;
p_var can store the address of the datatype ;
by incrementing the p_var we go next location of the datatype that is gets incremented 
by the value of size of datatype ;
*p_var givaes value at p_var i.e value of var ;

double pointers ;
to store the address of pointers we use double pointers 
dec: datatype **pp_var = &p_var ;
*pp_var gives value at pp_var i.e addr of var or value of p_var ;
**pp_var gives value at *pp_var i.e var value ;

reference:
use this while passing & ;
*/